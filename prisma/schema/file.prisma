model File {
  id        Int      @id @default(autoincrement())
  filename  String
  url       String
  mimetype  String?
  size      Int?
  userId    Int?
  name      String? // <- added for custom file name
  userName  String? // <- added
  user      User?    @relation(fields: [userId], references: [id], onDelete: Cascade)
  createdAt DateTime @default(now())

  @@map("files")
}
